export enum InteractionType {
  Email = "email",
  Call = "call",
  Meeting = "meeting",
  Sms = "sms",
  Linkedin = "linkedin",
}

export enum InteractionDirection {
  Inbound = "inbound",
  Outbound = "outbound",
}
